#include<iostream>
#include<ncurses.h>

function Events(){
    char started = 'false';
    char keycode = key;
    char mousebutton = button;

    char OnStart() = 'if(started == true){}';
    char OnKey(key) = 'if (key == keycode){}';
    char OnClick() =
}

function ConsoleHelp(){
    char help(topic) = 'help(topic)';
    char topic = topic

    if topic = 'onstart'{
        cout << "OnStart is a function that is called when the program starts" << endl;
    }
}

